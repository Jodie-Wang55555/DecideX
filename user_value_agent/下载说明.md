# DecideX 代码下载说明

## 📦 快速下载

您的代码已打包完成，位于：
```
/tmp/decidex-cost-agent.tar.gz (70KB)
```

---

## 🚀 三种获取方式

### 方式一：直接下载文件
如果您有文件系统访问权限，直接下载：
```
/tmp/decidex-cost-agent.tar.gz
```

### 方式二：使用 Git 下载
```bash
# 1. 克隆仓库（替换为您的仓库地址）
git clone https://github.com/Jodie-Wang55555/DecideX.git

# 2. 进入项目目录
cd DecideX

# 3. 查看最新代码
ls -la
```

### 方式三：手动复制核心文件
如果无法下载压缩包，复制以下核心文件：

#### 必需文件清单：
```
1. src/agents/agent.py           - 成本分析 Agent 代码
2. config/agent_llm_config.json  - Agent 配置文件
3. src/main.py                   - HTTP 服务入口
4. requirements.txt              - Python 依赖
5. scripts/local_run.sh          - 本地测试脚本
6. scripts/http_run.sh           - HTTP 服务启动脚本
```

---

## 📋 包含的文件清单

压缩包包含以下内容：
- ✅ `src/` - 完整源代码（41个文件）
- ✅ `config/` - 配置文件
- ✅ `scripts/` - 运行脚本
- ✅ `requirements.txt` - 依赖清单
- ✅ `README.md` - 项目文档

---

## 🔧 在新环境中部署

```bash
# 1. 解压压缩包
tar -xzf decidex-cost-agent.tar.gz

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量
# 创建 .env 文件并填入：
# COZE_WORKLOAD_IDENTITY_API_KEY=your_api_key
# COZE_INTEGRATION_MODEL_BASE_URL=your_base_url

# 4. 启动服务
bash scripts/http_run.sh -m http -p 5000

# 5. 测试 Agent
bash scripts/local_run.sh -m agent -i "我想和朋友去旅行，帮我分析成本"
```

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. ✅ Python 版本：需要 Python 3.11+
2. ✅ 依赖安装：确保所有包已安装
3. ✅ 环境变量：检查 API Key 和 Base URL
4. ✅ 端口占用：确保 5000 端口未被占用
